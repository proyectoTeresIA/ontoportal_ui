- ontology_name = @ontology ? " on #{@ontology.name}" : ""
- @title = "#{t('notes.headers.subject')}#{ontology_name} | #{@note.subject}"
#note_container{:style => "padding: 1em;"}
  = render :partial => 'thread'
