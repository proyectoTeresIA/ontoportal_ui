-@title = t('virtual_appliance.title', default: 'OntoPortal Virtual Appliance Download')

%div{:style => "padding: 13px; max-width: 800px;", :class => "enable-lists"}
  %h1= t('virtual_appliance.heading', default: 'OntoPortal Virtual Appliance Download')
  %p
    = t('virtual_appliance.description_intro')
  %p= t('virtual_appliance.description_included')
  %ul
    %li= t('virtual_appliance.included.ontologies_api')
    %li= t('virtual_appliance.included.annotator')
    %li= t('virtual_appliance.included.recommender')
    %li= t('virtual_appliance.included.bioportal_ui')
  %p
    = t('virtual_appliance.please_see_our')
    =link_to t('virtual_appliance.documentation'), 'https://ontoportal.github.io/documentation/administration/steps/getting_started', target: '_blank'
    = t('virtual_appliance.more_info_appliance')
  %h2= t('virtual_appliance.download_latest', default: 'Download Latest OntoPortal')
  %ul
    %li
      #{t('virtual_appliance.version_label')} 3.2.2
      <a href="https://www.bioontology.org/ontoportal-appliance/ontoportal_appliance-vmware-3.2.2.ova"> #{t('virtual_appliance.ova')}</a>
      |
      <a href="https://ontoportal.github.io/documentation/administration/steps/getting_started#amazon-aws-ami"> #{t('virtual_appliance.amazon_ec2')}</a>
    %li
      #{t('virtual_appliance.version_label')} 3.2.1
      <a href="https://www.bioontology.org/ontoportal-appliance/ontoportal_appliance-vmware-3.2.1.ova"> #{t('virtual_appliance.ova')}</a>
      |
      <a href="https://ontoportal.github.io/documentation/administration/steps/getting_started#amazon-aws-ami"> #{t('virtual_appliance.amazon_ec2')}</a>
    %li
      #{t('virtual_appliance.version_label')} 3.2.0
      <a href="https://www.bioontology.org/ontoportal-appliance/ontoportal_appliance-vmware-3.2.0.ova"> #{t('virtual_appliance.ova')}</a>
      |
      <a href="https://ontoportal.github.io/documentation/administration/steps/getting_started#amazon-aws-ami"> #{t('virtual_appliance.amazon_ec2')}</a>
    %li
      #{t('virtual_appliance.version_label')} 3.1.1
      <a href="https://www.bioontology.org/ontoportal-appliance/ontoportal_appliance-vmware-3.1.1.ova"> #{t('virtual_appliance.ova')}</a>
      |
      <a href="https://ontoportal.github.io/documentation/administration/steps/getting_started#amazon-aws-ami"> #{t('virtual_appliance.amazon_ec2')}</a>
    %li
      #{t('virtual_appliance.version_label')} 3.1.0
      <a href="https://www.bioontology.org/ontoportal-appliance/ontoportal_appliance-vmware-3.1.0.ova"> #{t('virtual_appliance.ova')}</a>
      |
      <a href="https://ontoportal.github.io/documentation/administration/steps/getting_started#amazon-aws-ami"> #{t('virtual_appliance.amazon_ec2')}</a>
    %li
      #{t('virtual_appliance.version_label')} 3.0.6
      <a href="https://www.bioontology.org/ontoportal-appliance/ontoportal_appliance-vmware-3.0.6.ova"> #{t('virtual_appliance.ova')}</a>
      |
      <a href="https://ontoportal.github.io/documentation/administration/steps/getting_started#amazon-aws-ami"> #{t('virtual_appliance.amazon_ec2')}</a>
    %li
      #{t('virtual_appliance.version_label')} 3.0.5
      <a href="https://www.bioontology.org/ontoportal-appliance/ontoportal_appliance-vmware-3.0.5.ova"> #{t('virtual_appliance.ova')}</a>
      |
      <a href="https://ontoportal.github.io/documentation/administration/steps/getting_started#amazon-aws-ami"> #{t('virtual_appliance.amazon_ec2')}</a>
    %li
      #{t('virtual_appliance.version_label')} 3.0.4
      <a href="https://www.bioontology.org/ontoportal-appliance/ontoportal_appliance-vmware-3.0.4.ova"> #{t('virtual_appliance.ova')}</a>
      |
      <a href="https://ontoportal.github.io/documentation/administration/steps/getting_started#amazon-aws-ami"> #{t('virtual_appliance.amazon_ec2')}</a>
    %li
      #{t('virtual_appliance.version_label')} 3.0.3
      <a href="https://www.bioontology.org/ontoportal-appliance/ontoportal_appliance-vmware-3.0.3.ova"> #{t('virtual_appliance.ova')}</a>
      |
      <a href="https://ontoportal.github.io/documentation/administration/steps/getting_started#amazon-aws-ami"> #{t('virtual_appliance.amazon_ec2')}</a>
    %li
      #{t('virtual_appliance.version_label')} 3.0.2
      <a href="https://www.bioontology.org/ontoportal-appliance/ontoportal_appliance-vmware-3.0.2.ova"> #{t('virtual_appliance.ova')}</a>
      |
      <a href="https://ontoportal.github.io/documentation/administration/steps/getting_started#amazon-aws-ami"> #{t('virtual_appliance.amazon_ec2')}</a>

  -if @virtual_appliance_access
    %h3= t('virtual_appliance.archives')
    %p
      = t('virtual_appliance.archives_paragraph')
    %ul
      %li
        #{t('virtual_appliance.version_label')} 2.5
        =link_to t('virtual_appliance.ovf'), 'https://www.bioontology.org/ontoportal-appliance/ontoportal-2.5.zip'

  -if @user.admin?
    %div{:style => "margin: 2em 0 0; padding: 1em 1em; border: solid thin gray; background-color: lightGray;"}
      %h1{:style => "margin-bottom: 15px;"}= t('virtual_appliance.admin.add_users_heading')
      =form_for :appliance_user do |f|
        = f.label :user_id, t('virtual_appliance.admin.account_name_label')
        = f.text_field :user_id
        &nbsp;&nbsp;
        = f.submit t('virtual_appliance.admin.add_user_button')

      %h2{:style => "margin-top: 1em;"}
        = t('virtual_appliance.admin.accounts_with_access')
        %a#export{href: "#", style: "margin-left: 2em; margin-top: -1em; font-size: 11px;"}
          = t('virtual_appliance.admin.export_users_csv')
      %table#user_table.zebra{style: "border: thin lightGray solid; background-color: white;"}
        %thead
          %tr
            %th= t('virtual_appliance.admin.user_id_header')
        %tbody
        -@va_users.each do |u|
          %tr
            %td= link_to nil, u.user_id, target: '_blank'

:javascript

  $(document).ready(function() {
    function exportTableToCSV($table, filename) {
      var $headers = $table.find('tr:has(th)'),
          $rows = $table.find('tr:has(td)'),
          // Temporary delimiter characters unlikely to be typed by keyboard
          // This is to avoid accidentally splitting the actual contents
          tmpColDelim = String.fromCharCode(11), // vertical tab character
          tmpRowDelim = String.fromCharCode(0), // null character
          // actual delimiter characters for CSV format
          colDelim = '","',
          rowDelim = '"\r\n"',
          // Grab text from table into CSV formatted string
          csv = '"',
          csvData;
      csv += formatRows($headers.map(grabRow));
      csv += rowDelim;
      csv += formatRows($rows.map(grabRow)) + '"';

      // Data URI
      csvData = 'data:application/csv;charset=utf-8,' + encodeURIComponent(csv);
      $(this).attr({
        'download': filename,
        'href': csvData
      });

      //------------------------------------------------------------
      // Helper Functions
      //------------------------------------------------------------

      // Format the output so it has the appropriate delimiters
      function formatRows(rows) {
          return rows.get().join(tmpRowDelim).split(tmpRowDelim).join(rowDelim).split(tmpColDelim).join(colDelim);
      }

      // Grab and format a row from the table
      function grabRow(i, row) {
        var $row = $(row);
        //for some reason $cols = $row.find('td') || $row.find('th') won't work...
        var $cols = $row.find('td');
        if (!$cols.length) $cols = $row.find('th');
        return $cols.map(grabCol).get().join(tmpColDelim);
      }

      // Grab and format a column from the table
      function grabCol(j, col) {
        var $col = $(col),
            $text = $col.text();
        return $text.replace('"', '""'); // escape double quotes
      }
    }

    // This must be a hyperlink
    $("#export").click(function(event) {
      var outputFile = 'appliance_users.csv'
      exportTableToCSV.apply(this, [$('#user_table'), outputFile]);
    });
  });
